{
  "name": "demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@githubprimer/octicons-react": "^8.1.3",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-guitar-chords": "^1.0.0",
    "react-scripts": "2.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && cd build; surge --domain react-guitar-chords.surge.sh"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "surge": "^0.20.1"
  }
}
